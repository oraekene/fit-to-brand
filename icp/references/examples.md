# Paired examples — QMD vs AIDE voice (keep schemas tight, read this for tone; O-GTM unless tagged)

## S2 — record vs choice (both columns required)
1. Medication — need: `Medication management: doses, interactions, refills, side-effect history` / moment: `whether to start/stop medication, choosing between treatment options`.
2. Career — need: `Job application tracker: versions of resumes, cover letters, outcomes` / moment: `Which job offer to accept, whether to quit, whether to ask for a raise`.
3. Housing — need: `Paint colors, flooring, materials used in each room` / moment: `buy vs rent`.
4. Subscriptions — need: `Subscription inventory and what each costs` / moment: `subscription sign-up/cancellation`.
5. Salary — need: `Salary negotiation research and past negotiations` / moment: `salary negotiation between employer and employee`.
6. Investment — need: `Investment decisions with rationale ("why did I buy this in 2021?")` / moment: `which investments to make, asset allocation`.
7. Negotiation — need as artifact: `bridewealth records, vendor scorecards, resolution precedents` / moment as event: `splitting, agreeing terms, resolving a dispute`.

## S3 — direct ticket vs failure cost
1. National election — direct `T13 $0 / no_direct_ticket (vote itself moves no money)` vs failure `T0 systemic (legitimacy loss)`.
2. Peace treaty — direct `$0 procedural` vs failure `T0 unbounded`.
3. IP docketing deadline — direct `$2K filing` vs failure `T1 $100M lapse`.
4. Household economics (aligns) — retirement `$500-600K`, buy-vs-rent `$300K`, job offer `$60K` on both lenses.
5. Priceless vs trivial $0 — `T8 priceless: oral histories, grief docs, ER-triage` vs `T13 $0: split bill $2K, where-to-eat $300, forgive someone`.

## S4 — catalog voice
1. Clio — QMD row: `legal practice management` / AIDE leaf: `intake pipeline: Translation System (§5) parses NL to Stakeholder/Leverage/Red-Line JSON`.
2. SAP/Oracle — QMD: `generic ERP` / AIDE: `Utility Quantification Layer for capex (AHP + minimax)`.
3. Relativity — QMD: `eDiscovery platform` / AIDE: `post-review Negotiation Strategy Module (Expected Value v3.3)`.
4. Services gap — AIDE header pattern `34 leaves (23 services + 11 products)`; QMD S4 carries zero services (4a gap — always run 4b).
5. Physical product row — `P-0601 | power tools | product | physical | DeWalt/Makita 18V drills | T5-T7 | B3 Small Business | S-0712,S-0713 | $99-249 + $49 battery attach | retail/dealer | 3-yr warranty, UL/CE`.

## S5A — fit voice (concrete + cite, both required)
1. `(a)` QMD: `Hostfully/HomeZada/Helpjuice/Evernote free local-first swap` / AIDE: `life/coaching and community-mediation automation` / physical: `(a) 18V drill full-SKU swap on torque-per-dollar`.
2. `(b)` QMD: `post-VDR export to deal silo; MCP consults runbooks` / AIDE: `BATNA floor in DocuSign redlining; minimax hedge in Bloomberg` / physical: `(b-ACC) battery pack as consumable attach in aisle bay 4`, `(b-SERV) install + 3-yr warranty wrapper at checkout work order`.
3. NOT-fit — QMD §G refuses `passwords, elections, SCADA, GxP, docketing math` with reasons; AIDE run gave even SCADA/911 a `(b)` (overclaim — gate red); physical: mains-panel work without UL listing ships `X` with cert note.

## S5B — segment wording (same fit, two bodies)
Fit `(S-0401 Notion, b-ING post-export to deal silo)`: prosumer body `your brief bank lives beside the system of record; token rotates after resale` vs ENT body `practitioner memory layer beside VDR; MCP-grounded runbooks; zero retraining`.

## Agnosticism probes (v1.3)
- Embodied lens — bleed control: `knowledge_need: N/A (no retrieval step; reason: time-critical psychomotor)` / `decision_moment: which pressure point` / `action_moment: apply direct pressure, reassess at 3 min`. Harvest timing: need `N/A: phenology memory lives in co-op ledger, not this tool` / moment `which field first` / action `cut at 18% moisture before rain front`.
- Sector tags — same A7 household row tagged `SEC-INFORMAL` (unlicensed childcare trust chains) vs `SEC-REGULATED` (licensed daycare ratios); mining instrumentation row tagged `SEC-MINING`.
- PPP note — sharecropping terms: `tier_decade=T10` + `price_note: $60/season nominal; 3 weeks staple-equivalent, PPP-adjusted` + livelihood tier.
- Category-mode — S0 `run-mode: category (all 18V drills)`; `(a)` reads `share-shift from DeWalt 18V on torque-per-dollar wedge`, never self-`(a)`; Top-10 ranks wedges.

## Overlay renders (same core, alternate wording)
- O-OPS — fit `(S-1101 river crossing, P-0201 pontoon set, b-COMP)`: landing-place `second wave at crossing site Bravo per engineer tasking §4.2`; group `bridging cell`; brief `task: span by 0600 / intent: armor passes / constraint: no daylight recon`.
- O-EDU — fit `(S-0712 fractions unit, P-0104 fraction tiles, b-ACC)`: landing-place `week-3 station rotation in room 12 per scheme §2`; group `grade-4 blue cohort`; brief `objective: equivalent fractions / checks: exit tickets ×3 / assignment: tile workbook p.14`.
